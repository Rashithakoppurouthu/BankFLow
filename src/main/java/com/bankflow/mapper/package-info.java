/**
 * Mapper Layer:
 * 
 * - Responsible for converting between JPA Entities and Data Transfer Objects (DTOs).
 * - Implemented with clean, explicit Java mapping logic so freshers can easily debug and trace field transformations.
 * - Keeps conversion logic centralized and decoupled from both controllers and services.
 */
package com.bankflow.mapper;
